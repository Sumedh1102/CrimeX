"""Official aggregated statistics (Brihan Mumbai Police monthly statement).

This layer is strictly separate from incident-level data: it holds city-level
aggregate counts exactly as printed in the source PDF, with provenance. It is
never used as incident data and cannot, on its own, train a spatial model.
"""
